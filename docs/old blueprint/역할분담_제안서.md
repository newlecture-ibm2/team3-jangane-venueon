# 🎯 VenueOn 5인 역할 분담 제안서

> **작성일:** 2026-03-29  
> **목표:** 파일 충돌 최소화 + 백엔드/프론트엔드 도메인별 수직 분할  
> **기반 문서:** `MVP_아키텍처_v3.md`, `VenueOn_WBS.csv`

---

## 📐 1. 분배 원칙 및 이유

### 원칙 1: 도메인 수직 분할 (Vertical Slicing)

```
❌ 수평 분할 (역할별)              ✅ 수직 분할 (도메인별)
┌──────────────────────┐        ┌──────┬──────┬──────┐
│  FE 담당 2명          │        │ 인증  │이벤트│커뮤  │
│  (모든 페이지)        │        │ BE+FE│BE+FE│BE+FE │
├──────────────────────┤        │  A   │  B  │  D   │
│  BE 담당 2명          │        │      │     │      │
│  (모든 API)           │        │      │     │      │
└──────────────────────┘        └──────┴──────┴──────┘
→ FE↔BE 인터페이스 합의 必        → 한 사람이 API+UI 모두 책임
→ 커뮤니케이션 비용 ↑              → 커뮤니케이션 비용 ↓
→ 같은 파일 동시 수정 빈번         → 파일 충돌 거의 없음
```

**이유:**
- 헥사고날 아키텍처 + 모듈러 모놀리스 구조에서는 도메인 모듈(`user/`, `event/`, `community/`)이 **패키지 단위로 완전히 분리**되어 있음
- 한 사람이 백엔드 API와 프론트엔드 UI를 모두 담당하면 **BE↔FE 인터페이스 합의 불필요** → 개발 속도 ↑
- Next.js의 App Router 구조도 **폴더 기반 라우팅**이라 도메인별로 폴더가 나뉘어 있어 충돌 없음

### 원칙 2: 공통 모듈 전담자 지정

**이유:**
- `common/` 패키지(ApiResponse, Security, JWT 필터 등)와 공통 UI(`Header`, `Footer`, `Button` 등)는 **모든 팀원이 의존**하는 핵심 기반
- 여러 명이 동시에 수정하면 **머지 충돌의 주범**이 됨
- **1명이 전담**하여 Sprint 0에서 빠르게 완성 → 나머지 팀원은 안정된 기반 위에서 개발

### 원칙 3: event/ 모듈 내부를 파일 단위로 분할

**이유:**
- `event/` 모듈은 이벤트 CRUD + 주문/결제를 모두 포함하여 **작업량이 가장 많음**
- 하지만 Event 엔티티와 Order 엔티티가 **완전히 다른 파일**로 구성됨
- 같은 패키지(`event/`) 안에 있지만 `EventController.java`와 `OrderController.java`처럼 **파일이 다르므로 Git 충돌 없음**

---

## 👥 2. 5인 역할 분배

---

### 담당자 A: 🔧 공통 인프라 + 인증 + 마이페이지

> **역할 요약:** 모든 팀원이 의존하는 기반을 먼저 만들어주는 "기둥" 역할

#### 담당 범위

| 구분 | 담당 파일/폴더 |
|------|---------------|
| **BE 공통** | `common/dto/ApiResponse.java` |
| **BE 공통** | `common/exception/GlobalExceptionHandler.java`, `ErrorCode.java` |
| **BE 공통** | `common/annotation/UseCase.java` |
| **BE 공통** | `common/config/SecurityConfig.java`, `JwtAuthFilter.java`, `SwaggerConfig.java` |
| **BE User** | `user/` 모듈 전체 — domain, application, adapter |
| **BE 기타** | `VenueOnApplication.java`, `application.properties` |
| **FE** | `(auth)/login/page.tsx`, `(auth)/signup/page.tsx` |
| **FE** | `(auth)/components/` — LoginForm, SignupForm, useAuth |
| **FE** | `mypage/` 전체 — page.tsx, events/, tickets/, communities/ |
| **FE** | `lib/` — api.ts, auth.ts, utils.ts |
| **인프라** | `infra/docker-compose.yml`, `.env` |
| **인프라** | `.github/workflows/ci.yml` |

#### 담당 API (9개)

| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/auth/signup` | 회원가입 |
| POST | `/auth/login` | 로그인 → JWT 발급 |
| POST | `/auth/refresh` | Access Token 갱신 |
| POST | `/auth/logout` | 로그아웃 (토큰 블랙리스트) |
| GET | `/mypage/profile` | 내 프로필 조회 |
| PUT | `/mypage/profile` | 내 프로필 수정 |
| GET | `/mypage/orders` | 내 구매/예약 내역 |
| GET | `/mypage/communities` | 내 커뮤니티 목록 |
| GET | `/mypage/events` | 내 참여 이벤트 목록 |

#### 이 역할에 이 사람이 적합한 이유

- Sprint 0에서 **가장 먼저 완성**해야 하는 인프라(Docker, Security, JWT)를 담당
- `common/` 모듈을 한 명이 전담하면 **다른 팀원이 공통 코드를 건드릴 필요 없음**
- `user/` 모듈과 `common/` 모듈은 **의존 관계가 가장 깊음** (SecurityConfig → JWT → User) → 한 명이 묶어서 하면 효율적

#### WBS 매핑 태스크

- `#6` Spring Boot 프로젝트 생성
- `#8` CORS + 설정 파일
- `#9` JPA Entity + 이니셜라이저
- `#11` 이미지 업로드 API
- `#12` User API (BE)
- `#13` 로그인/가입 페이지 (FE)
- `#14` 인증 연동 검증
- `#15` GitHub Actions 배포
- `#24` 마이페이지 UI (FE)

---

### 담당자 B: 📅 이벤트 CRUD

> **역할 요약:** 이벤트의 생성·조회·수정·삭제 + 검색/필터 + 이벤트 관련 UI 전체

#### 담당 범위

| 구분 | 담당 파일/폴더 |
|------|---------------|
| **BE Domain** | `event/domain/model/Event.java`, `EventStatus.java` |
| **BE Domain** | `event/domain/exception/EventDomainException.java` |
| **BE Port In** | `CreateEventUseCase.java`, `GetEventUseCase.java`, `UpdateEventUseCase.java`, `DeleteEventUseCase.java`, `SearchEventUseCase.java` |
| **BE Service** | 위 UseCase에 대응하는 5개 Service |
| **BE DTO** | `CreateEventCommand.java`, `UpdateEventCommand.java`, `EventInfo.java` |
| **BE Port Out** | `LoadEventPort.java`, `SaveEventPort.java`, `UserQueryPort.java` |
| **BE Adapter In** | `EventController.java` + dto/request, response |
| **BE Adapter Out** | `EventJpaEntity.java`, `EventJpaRepository.java`, `EventPersistenceAdapter.java`, `EventMapper.java` |
| **BE Internal** | `UserQueryAdapter.java` |
| **FE** | `events/page.tsx` — 이벤트 탐색(목록) 페이지 |
| **FE** | `events/[id]/page.tsx` — 이벤트 상세 페이지 |
| **FE** | `events/new/page.tsx` — 이벤트 생성/수정 폼 |
| **FE** | `events/components/EventList.tsx`, `EventDetail.tsx`, `EventForm.tsx`, `useEvents.ts` |

#### 담당 API (6개)

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/events` | 이벤트 목록 (검색/필터/페이징) |
| GET | `/events/{id}` | 이벤트 상세 |
| POST | `/host/events` | 이벤트 생성 |
| PUT | `/host/events/{id}` | 이벤트 수정 (본인만) |
| DELETE | `/host/events/{id}` | 이벤트 삭제 (본인만) |
| PATCH | `/host/events/{id}/status` | 상태 변경 (DRAFT→PUBLISHED) |

#### 이 역할에 이 사람이 적합한 이유

- 이벤트는 VenueOn의 **핵심 도메인** → 가장 많은 비즈니스 로직(상태 관리, 검색, 필터 등)
- `EventController` ↔ `OrderController`는 **완전히 다른 파일**이므로 담당자 C와 충돌 없음
- BE Event 도메인 + FE 이벤트 페이지를 한 명이 담당하면 **API 스펙 변경 시 양쪽 즉시 반영** 가능

#### WBS 매핑 태스크

- `#18` Event API (BE)
- `#19` 메인 홈 (FE) — E와 협업, 이벤트 카드 컴포넌트 제공
- `#20` 이벤트 상세 + 간소화 등록 폼 (FE)

---

### 담당자 C: 🎫 티켓/결제(주문) + 기업/기관 페이지

> **역할 요약:** 주문·결제 도메인 + Host(기업) 프로필/대시보드

#### 담당 범위

| 구분 | 담당 파일/폴더 |
|------|---------------|
| **BE Domain** | `event/domain/model/Order.java`, `OrderStatus.java` |
| **BE Port In** | `CreateOrderUseCase.java`, `CancelOrderUseCase.java`, `GetMyOrdersUseCase.java` |
| **BE Service** | `CreateOrderService.java`, `CancelOrderService.java`, `GetMyOrdersService.java` |
| **BE DTO** | `CreateOrderCommand.java`, `OrderInfo.java` |
| **BE Port Out** | `LoadOrderPort.java`, `SaveOrderPort.java` |
| **BE Adapter In** | `OrderController.java` + dto/ |
| **BE Adapter Out** | `OrderJpaEntity.java`, `OrderJpaRepository.java`, `OrderPersistenceAdapter.java`, `OrderMapper.java` |
| **BE Host** | `host/` 모듈 전체 (HostProfile, Dashboard 등) |
| **FE** | `events/components/TicketSection.tsx`, `useOrder.ts` |
| **FE** | `org/` (기업/기관 페이지) — `[id]/page.tsx`, `dashboard/page.tsx` |
| **FE** | `mypage/components/OrderHistory.tsx` |

#### 담당 API (9개)

| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/orders` | 참가 신청 (더미 결제) |
| GET | `/orders/{id}` | 주문 상세 (본인만) |
| POST | `/orders/{id}/cancel` | 참가 취소 |
| GET | `/host` | Host 대시보드 |
| GET | `/host/profile` | 기업 프로필 조회 |
| PUT | `/host/profile` | 기업 프로필 수정 |
| GET | `/host/events` | 내가 주최한 이벤트 목록 |

#### 이 역할에 이 사람이 적합한 이유

- Order(주문)는 Event에 종속되지만 **파일이 완전히 분리**됨 → B와 충돌 없음
- 기업/기관 페이지(`host/` 모듈)는 **독립 모듈**로 아무도 건드리지 않아 단독 개발 가능
- 결제(더미) → 주문 내역 → 기업 대시보드는 **데이터 흐름이 연결**되므로 한 사람이 일관성 있게 구현

#### 담당자 B와의 경계 (event/ 모듈 내부)

```
event/
├── domain/model/
│   ├── Event.java          ← B 담당
│   ├── EventStatus.java    ← B 담당
│   ├── Order.java          ← C 담당 ✅ 파일 다름
│   └── OrderStatus.java    ← C 담당 ✅ 파일 다름
├── application/
│   ├── port/in/
│   │   ├── CreateEventUseCase.java   ← B
│   │   ├── ...EventUseCase.java      ← B
│   │   ├── CreateOrderUseCase.java   ← C ✅ 파일 다름
│   │   └── ...OrderUseCase.java      ← C ✅ 파일 다름
│   └── service/
│       ├── ...EventService.java      ← B
│       └── ...OrderService.java      ← C ✅ 파일 다름
├── adapter/in/web/
│   ├── EventController.java          ← B
│   └── OrderController.java          ← C ✅ 파일 다름
└── adapter/out/persistence/
    ├── entity/
    │   ├── EventJpaEntity.java       ← B
    │   └── OrderJpaEntity.java       ← C ✅ 파일 다름
    └── ...
```

> **결론:** 같은 `event/` 패키지를 공유하지만, **모든 파일이 다르므로 Git 충돌이 발생하지 않습니다.**

#### WBS 매핑 태스크

- `#21` 더미 결제 (BE+FE)
- `#25` 기업/기관 페이지 (BE+FE)

---

### 담당자 D: 💬 커뮤니티 CRUD + 게시글 CRUD

> **역할 요약:** community 모듈 전체를 독점 — 가장 독립적인 도메인

#### 담당 범위

| 구분 | 담당 파일/폴더 |
|------|---------------|
| **BE Domain** | `community/domain/` 전체 — Community, CommunityMember, Post, Comment, PostType |
| **BE Port In** | 12개 UseCase (CRUD × Community/Post/Comment + Join/Leave) |
| **BE Service** | 12개 Service 구현체 |
| **BE DTO** | CreateCommunityCommand, CreatePostCommand, CommunityInfo, PostInfo |
| **BE Port Out** | LoadCommunity, SaveCommunity, LoadPost, SavePost, LoadComment, SaveComment, EventQueryPort, UserQueryPort |
| **BE Adapter In** | `CommunityController.java`, `PostController.java`, `CommentController.java` + dto/ |
| **BE Adapter Out** | persistence/ 전체 (entity, repository, adapter, mapper) |
| **BE Internal** | `EventQueryAdapter.java`, `UserQueryAdapter.java` |
| **FE** | `community/page.tsx` — 커뮤니티 목록 |
| **FE** | `community/[id]/page.tsx` — 커뮤니티 상세 |
| **FE** | `community/[id]/posts/new/page.tsx` — 게시글 작성 |
| **FE** | `community/[id]/posts/[postId]/page.tsx` — 게시글 상세 |
| **FE** | `community/components/` 전체 — CommunityList, PostList, PostForm, CommentList, useCommunity, usePosts |

#### 담당 API (16개)

| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/communities` | 커뮤니티 생성 |
| GET | `/communities` | 커뮤니티 목록 |
| GET | `/communities/{id}` | 커뮤니티 상세 |
| PUT | `/communities/{id}` | 커뮤니티 수정 (작성자만) |
| DELETE | `/communities/{id}` | 커뮤니티 삭제 (작성자만) |
| POST | `/communities/{id}/join` | 커뮤니티 가입 |
| DELETE | `/communities/{id}/leave` | 커뮤니티 탈퇴 |
| GET | `/communities/{id}/members` | 멤버 목록 |
| POST | `/communities/{cId}/posts` | 게시글 작성 |
| GET | `/communities/{cId}/posts` | 게시글 목록 |
| GET | `/posts/{id}` | 게시글 상세 |
| PUT | `/posts/{id}` | 게시글 수정 (작성자만) |
| DELETE | `/posts/{id}` | 게시글 삭제 (작성자만) |
| POST | `/posts/{postId}/comments` | 댓글 작성 |
| GET | `/posts/{postId}/comments` | 댓글 목록 |
| DELETE | `/comments/{id}` | 댓글 삭제 (작성자만) |

#### 이 역할에 이 사람이 적합한 이유

- `community/` 모듈은 **다른 모듈과 완전히 독립** (BE 패키지도, FE 폴더도 별개)
- API 수가 16개로 **가장 많지만**, 대부분 CRUD 패턴이 반복되어 **생산성이 높음**
- 다른 모듈을 참조할 때도 `internal/` 어댑터를 통해 **Port 인터페이스만 호출** → 직접 의존 없음
- **파일 충돌 가능성: 0%** — 다른 누구도 `community/` 폴더를 건드리지 않음

#### WBS 매핑 태스크

- `#22` Community · Post API (BE)
- `#23` 커뮤니티/게시판 UI (FE)

---

### 담당자 E: 🎨 공통 UI 컴포넌트 + 메인 홈 + Admin

> **역할 요약:** 프론트엔드 공통 기반과 랜딩 페이지를 책임 + Admin 기능

#### 담당 범위

| 구분 | 담당 파일/폴더 |
|------|---------------|
| **FE 공통** | `components/Button.tsx`, `Card.tsx`, `Modal.tsx`, `Header.tsx`, `Footer.tsx`, `Pagination.tsx` |
| **FE 스타일** | `styles/globals.css`, `styles/variables.css` (디자인 토큰) |
| **FE 레이아웃** | `app/layout.tsx` (루트 레이아웃) |
| **FE 메인홈** | `app/page.tsx`, `app/page.module.css` (메인 랜딩 페이지) |
| **BE Admin** | `admin/` 모듈 전체 (GetAllUsersUseCase, AdminUserController 등) |
| **BE Upload** | `common/adapter/in/web/UploadController.java` (이미지 업로드 API) |
| **FE Admin** | Admin 회원관리 페이지 |

#### 담당 API (2개)

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/admin/users` | 전체 회원 리스트 조회 (Admin) |
| POST | `/upload/image` | 이미지 업로드 |

#### 이 역할에 이 사람이 적합한 이유

- **디자인 시스템**(CSS 변수, 공통 컴포넌트)을 한 명이 통제하면 **UI 일관성** 보장
- `Header`, `Footer`, `Button` 등은 모든 팀원이 사용 → **한 명이 만들고 다른 사람은 import만**
- 메인 홈 페이지는 이벤트 카드(`EventList`)를 가져다 배치하는 구성 → B가 만든 컴포넌트를 조합
- Admin은 API 1개 + UI 1개로 **작업량이 적어** 공통 UI 작업과 현실적으로 병행 가능
- Sprint 0에서 **FE 기반을 A와 동시에 빠르게 완성**해야 함

#### WBS 매핑 태스크

- `#7` Next.js 프로젝트 생성
- `#10` 공통 레이아웃 (layout.tsx, Header, Footer, 글로벌 CSS)
- `#19` 메인 홈 (FE) — B와 협업
- `#26` Admin 회원관리 (BE+FE)

---

## 📊 3. 파일 충돌 매트릭스

| | A (인증) | B (이벤트) | C (주문/기업) | D (커뮤니티) | E (공통UI) |
|---|:---:|:---:|:---:|:---:|:---:|
| **A** | — | ✅ 없음 | ⚠️ 최소 | ✅ 없음 | ⚠️ 최소 |
| **B** | ✅ 없음 | — | ⚠️ 최소 | ✅ 없음 | ⚠️ 최소 |
| **C** | ⚠️ 최소 | ⚠️ 최소 | — | ✅ 없음 | ✅ 없음 |
| **D** | ✅ 없음 | ✅ 없음 | ✅ 없음 | — | ✅ 없음 |
| **E** | ⚠️ 최소 | ⚠️ 최소 | ✅ 없음 | ✅ 없음 | — |

### ⚠️ 최소 접점 상세 설명

| 접점 | 관계 | 내용 | 해결 방법 |
|------|------|------|----------|
| **A↔C** | mypage 내 OrderHistory | A가 `mypage/page.tsx` 레이아웃, C가 `OrderHistory.tsx` 컴포넌트 | A가 페이지에 `<OrderHistory />` import 한 줄만 추가. C가 컴포넌트 완성 후 A에게 알림 |
| **A↔E** | Header에서 인증 상태 | E가 `Header.tsx` 개발, A가 `useAuth` Hook 제공 | E가 Header에서 `useAuth()` import. 인터페이스(반환값)만 사전 합의 |
| **B↔C** | event/ 모듈 내부 | 같은 패키지에 파일 공존 | **파일이 완전히 다름** (Event*.java vs Order*.java). 새 파일 추가만 하므로 충돌 없음 |
| **B↔E** | 메인 홈에서 이벤트 표시 | E가 `app/page.tsx` 레이아웃, B가 `EventList`/`EventCard` 컴포넌트 제공 | E가 B의 컴포넌트를 import하여 배치. B가 컴포넌트 완성 후 E에게 알림 |

---

## ⏰ 4. Sprint별 작업 흐름

### Sprint 0 (3/27 ~ 4/3) — 설계 + 인프라 + 인증 — 6일

| 일자 | A (인증/인프라) | B (이벤트) | C (주문/기업) | D (커뮤니티) | E (공통UI) |
|------|----------------|-----------|-------------|------------|-----------|
| **3/27 (금)** | 전원: 요구사항 확정 + 화면설계 + 스프린트 플래닝 ||||
| 3/28~29 | 휴무 ||||
| **3/30 (월)** | ERD 설계 참여 | ERD 설계 참여 | ERD 설계 참여 | ERD 설계 참여 | ERD 설계 참여 |
| | API 스펙 정의 | API 스펙 정의 | API 스펙 정의 | API 스펙 정의 | API 스펙 정의 |
| **3/31 (화)** | BE 프로젝트 생성 | Event 도메인 설계 | Order 도메인 설계 | Community 도메인 설계 | FE 프로젝트 생성 |
| | common 모듈 구현 | Entity 클래스 작성 | Entity 클래스 작성 | Entity 클래스 작성 | 디자인 시스템 구축 |
| | CORS/Security 설정 | | | | globals.css, variables.css |
| **4/1 (화)** | JPA Entity 매핑 | Event API 선행 | Order API 선행 | Community API 선행 | 공통 컴포넌트 개발 |
| | DataInitializer | (Port/Service 골격) | (Port/Service 골격) | (Port/Service 골격) | Header, Footer, Button |
| **4/2 (수)** | User API (BE) 완성 | Event API 계속 | Host 모듈 선행 | Community API 계속 | Card, Modal, Pagination |
| | 로그인/가입 UI (FE) | | | | layout.tsx 완성 |
| **4/3 (목)** | 인증 E2E 검증 | Event API 완성 | Order API 선행 | Community API 완성 | GitHub Actions 협업 |
| | GitHub Actions | | | | |

> **핵심:** A가 common + Security + JWT를 3/31에 완성 → 나머지 팀원 4/1부터 바로 API 개발 가능  
> E가 FE 프로젝트 + 공통 컴포넌트를 3/31~4/2에 완성 → Sprint 1에서 모두가 UI 바로 개발 가능

### Sprint 1 (4/6 ~ 4/9) — 🎯 MVP 완성 — 4일

| 일자 | A (인증/인프라) | B (이벤트) | C (주문/기업) | D (커뮤니티) | E (공통UI) |
|------|----------------|-----------|-------------|------------|-----------|
| **4/6 (월)** | 마이페이지 API (BE) | Event API 완성 | Order API (BE) | Community API 완성 | 메인 홈 페이지 (FE) |
| **4/7 (화)** | 마이페이지 UI (FE) | 이벤트 상세+폼 (FE) | 더미 결제 (BE+FE) | Post/Comment API | 메인 홈 API 연동 |
| **4/8 (수)** | 마이페이지 완성 | 이벤트 FE 완성 | 기업 페이지 (BE+FE) | 커뮤니티/게시판 (FE) | Admin (BE+FE) |
| **4/9 (목)** | 전원: MVP E2E 검증 + 버그 픽스 + 스프린트 리뷰 & 회고 ||||

---

## 🛡️ 5. 충돌 방지 규칙

### Git 브랜치 전략

```
main
└── develop
    ├── feature/auth          ← A 전용
    ├── feature/event         ← B 전용
    ├── feature/order-host    ← C 전용
    ├── feature/community     ← D 전용
    └── feature/common-ui     ← E 전용
```

- 각자 **자신의 feature 브랜치에서만 작업**
- `develop`으로 머지할 때 **PR(Pull Request)** 사용
- 공통 파일 수정이 필요하면 **담당자에게 요청** 후 머지

### 공통 파일 수정 권한

| 파일 | 수정 권한자 | 다른 사람이 변경 필요 시 |
|------|-----------|----------------------|
| `common/` 패키지 전체 | **A만** | A에게 Slack/이슈로 요청 |
| `components/` (공통 UI) | **E만** | E에게 요청 |
| `styles/variables.css` | **E만** | E에게 요청 |
| `layout.tsx` | **E 주도** | A가 인증 부분만 PR 제출 |
| `package.json` (FE) | **E 주도** | 라이브러리 추가 시 E를 통해 |
| `build.gradle` (BE) | **A 주도** | 의존성 추가 시 A를 통해 |

### 모듈 간 인터페이스 약속

1. **Port 인터페이스를 먼저 정의** (Sprint 0에서 합의)
2. 예: B가 `UserQueryPort`를 `event/` 모듈에 정의 → A가 `user/` 모듈에서 해당 구현체 제공
3. `internal/` 어댑터에서 처리하므로 실제 파일은 각자 모듈에만 존재

---

## 📋 6. 최종 요약

```
┌─────────────────────────────────────────────────────────────────┐
│                    VenueOn 5인 역할 분배도                        │
│                                                                 │
│  Backend (Spring Boot)              Frontend (Next.js)          │
│  ┌────────────────────┐             ┌──────────────────────┐    │
│  │ A: common/ + user/ │─────────────│ A: (auth)/ + mypage/ │    │
│  ├────────────────────┤             ├──────────────────────┤    │
│  │ B: event/ (Event)  │─────────────│ B: events/           │    │
│  ├────────────────────┤             ├──────────────────────┤    │
│  │ C: event/ (Order)  │─────────────│ C: TicketSection     │    │
│  │    + host/         │─────────────│    + org/             │    │
│  ├────────────────────┤             ├──────────────────────┤    │
│  │ D: community/      │─────────────│ D: community/        │    │
│  ├────────────────────┤             ├──────────────────────┤    │
│  │ E: admin/ + upload │─────────────│ E: components/       │    │
│  │                    │             │    + styles/ + 메인홈 │    │
│  └────────────────────┘             └──────────────────────┘    │
│                                                                 │
│  ✅ 파일 충돌: 거의 0% (4개 최소 접점만 사전 합의 필요)            │
│  ✅ 각자 독립 개발 가능 (Sprint 0에서 A·E가 기반 완성 후)          │
│  ✅ 도메인 수직 분할로 BE↔FE 커뮤니케이션 비용 최소화              │
└─────────────────────────────────────────────────────────────────┘
```
